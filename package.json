{
  "name": "package-based",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "run-all": "npx nx run-many --target=build",
    "run-all-no-cache": "npx nx run-many --target=build --skip-nx-cache",
    "run-all-affected": "npx nx affected --target=build"
  },
  "private": true,
  "devDependencies": {
    "@nrwl/nx-cloud": "latest",
    "nx": "15.0.13",
    "prettier": "^2.6.2",
    "typescript": "^4.8.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
